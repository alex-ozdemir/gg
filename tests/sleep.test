#!/bin/bash -xe

cd ${TEST_TMPDIR}

export PATH=${abs_builddir}/../src/models:${abs_builddir}/../src/frontend:$PATH

${abs_srcdir}/../examples/sleep/make-test.sh ${abs_builddir}/../examples/sleep/sleep ${abs_builddir}/../examples/sleep/merge
GG_FORCE_NO_STATUS=1 gg-force out
# Check output
diff out <(echo 'SLEEP(5)')
